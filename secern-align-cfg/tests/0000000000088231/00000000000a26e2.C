#include<stdio.h>
#include<string.h>
void eliminate(int n)
{
    
}

void main()
{
    int n,n1,i;
    scanf("%d \t",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d \t",&n1);
        eliminate(n1);
    }
}