#include <studio.h>
#include <convo.h>
void main()
{
    int a,b,A,B
    printf ("Enter the num:");
    if(a>=4)
    {
        b=a/2;
        if (a%2==0)
           A=b;
           B=b;
           Printf("%d %d",A,B);
           else
           {
               A=b;
               B=a-b;
               printf ("%d %d",A,B);
               getch ();
           }