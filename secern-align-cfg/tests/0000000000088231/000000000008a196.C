#include<stdio.h>
int main()
{
int test n,k,num;
scanf("%d",&test);
scanf("%d",&num);
for(int i=0;i<test;i++)
{
check(num);
}}
long int check(int num)
{
while(n!=0)
{
    k=n%10;
    n=n/10;
    if(k==4)
    {
        num=num/2;
        printf("%d %d",num,num);
    }
}