#include<stdio.h>
void main()
{
    int a,b,n,i,h;
    scanf("%d",&h);
    for(i=0;i<h;i++)
    {  
       scanf("%d",&n);
    }
    for(i=0;i<h;i++)
    {   
       a+b==n;
       if((a+b==n)&&(n==4))
         {
             printf("%d Case #i: %d %d",a,b);
         }
    }
}