#include<stdio.h>
int main()
{
    int n,a[1000];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    scanf("%d",&a[i]);
    for(i=0;i<n;i++)
    {
        j=1;
        k=a[i]-1;
        while(k>0)
        {
            if(k%10==4)
            {
                
            }
            
        }
    }
}