#include <stdio.h>
#include <math.h>

int main(){
    int n_cases, case_num = 1;
    scanf("%d", &n_cases);
    while(case_num++!=n_cases){
        int target;
        scanf("%d", &target);
        int a = target-1;
        printf("Case #%d: %d %d", case_num, a, 1);
        
    }
}