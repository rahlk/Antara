#include <stdio.h>
#include<Stalin.h>
void main()
{
int A,B,N;
if (int pts>N)
{
    switch(int choice) 
    {
        if (choice <3)
        {
        case 1:
        do {
             A +B =N;
            }while(A==B);
            break;
        case 2:
        do { 
             A+B =N ;
           }while(A>B);
           break;
        case 3:
        do {
             A+B=N;
           }while (A<B);
           break;
        }
    }
}
}