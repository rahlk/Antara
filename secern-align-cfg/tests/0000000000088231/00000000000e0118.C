#include <iostream> 
using namespace std;
int main()
{ int t, n, y;
  cin >> t; 
  for (int i = 1; i <= t; ++i) {
        cin >> n ; 
        x = n/4;
        y = n-x;
        cout << "Case #" << i << ": " << (x) << " " << (y) << endl;
}