#include<stdio.h>
#include<conio.h>
int main()
    {
    intN[q],i,j,check=0;
    int A[10];
    int B[10];
    printf("you have won jamcoins")
    printf("No of jamcoins");
    scanf("%d",&N[q]);
    printf("jamcoins in first ticket")
    scanf("%d",&A[i]);
    printf("jamcoins in second ticket")
    scanf("%d",&B[j]);
    for(i=0;i<=strlen(A);i++)
    {
        for(j=0;j<=strlen(B);j++)
        {
            for(q=0;q<=strlen(N);q++)
            {
            if(N[q]!=4)
            {
             z=A[i]+B[j];
             if(z==4)
             {
             check=1;
             printf(" 4 is there in jamcoins")
             }
             else
             {
             printf("not there");
             }}}}
             getch;
             }
             
        
        
        
                

                
        