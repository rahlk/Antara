#include<stdio.h>
#include<stdio.lib>
int main()
{
    int arr[10000]
    int N;
    if A+B=N;
    for(N=0;N<10000;N++)
    {
        printf("enter a value to element %d : ", N+1)
        scanf("%d", arr[N]);
    }
    printf("array values are\n");
    for(N=0;N<10000;N++)
    printf("%d",arr[N])
    return 0;
}