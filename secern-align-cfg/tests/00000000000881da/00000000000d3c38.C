#include<stdio.h>
void main(){
    int i,t,n,char[10];
    scanf("%d",&t);
    for(i=0;i<t;i++)
    {
        scanf("%d",&n);
        scanf("%s",p);
        for(j=0;j<strlen(p);j++)
            if(p[j]=='S')
                printf("E");
            else
                printf("S");
    }
}
