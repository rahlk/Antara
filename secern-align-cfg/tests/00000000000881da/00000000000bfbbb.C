#include<stdio.h>
int main(){
    int n,a,b,i;
    scanf("%d",&n);
    while(n>0){
        scanf("%d",&a);
        if(a==2)
            printf("SE");
        if(a==5)
            printf("EESSSESE");
        n--;
    }
}