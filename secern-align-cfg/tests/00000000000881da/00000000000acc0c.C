#include<stdio.h>
#include<string.h>
void main()
{
    
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
    
       char a[9000];
       scanf("%s"&s);
       l=strlen(s);
       for(k=0;k<l;k++)
       { if(a[k]=='S')
           { ola++;}
           else
           { bola++;}
       }
      while(s!='')
      { for(j=0;j<l;j++)
          
          if(n%2==0)
          {printf("%c",a[j]);
           
          }
      }
    }
    
    
    
    
    
    
    
    
    
    
    
    
    
}