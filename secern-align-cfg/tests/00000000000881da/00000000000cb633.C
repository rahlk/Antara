#include<stdio.h>
#include<iostream.h>
#include<stdlib.h>
#include<string.h>
int main(){
int t;
cin>>t;
string s;
int * p=(int *)malloc(t*sizeof(int));
for(int i=0;i<t;i++){
{p[i];
gets(s);}
int k=0;
for(int i=strlen(s);i>0;i++)
{cout<<"case #k+1"<<s[i];
    
}
free(p);
    
return 0;}
