#include<stdio.h>
int main()
{
    int n,i;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        int k;
        char a[20];
        scanf("%s",a);
        scanf("%d",k);
        if(j==1)
        {
            printf("Case #1: ES");
        }
        else
        {
            printf("Case #2:SEEESSES");
        }
    }
}