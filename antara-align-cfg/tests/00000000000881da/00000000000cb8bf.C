#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(){
int t;
scanf("%d",&t);
string s;
int * p=(int *)malloc(t*sizeof(int));
for(int i=0;i<t;i++){
{p[i];
gets(s);}
int k=0;
for(int i=strlen(s);i>0;i++)
{printf("%dcase #k+1",s[i];
    
}
free(p);
    
return 0;}
