#include<stdio.h>
int main()
{
    int T, N;
    char P[50000];
    scanf("%d", &T);
    for(int i=0; i<T; i++)
    {
        scanf("%d", &N);
        for(int j=0; j<N; j++)
        {
            scanf("%c", &p[j]);
        }
    
    for(int j=0; j<N;j++)
    {
        if(p[j]=='E')
            p[j]='S';
        else if(p[j]=='S')
            p[j]='E';
        
        
    }
    printf("Case #d: %s",i, p)
    }
    



}