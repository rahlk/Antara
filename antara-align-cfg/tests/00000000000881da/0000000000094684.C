#include <stdio.h>

int main() {
   int cases;
   scanf("%d", cases);
   for(int n = 1; n <= cases; n++) {
        int to_print;
        scanf("%d", to_print);
        printf("Case #%d: ", n);
        for(int i = 0; i < to_print; n++) {
            char = c;
            getchar(c);
            if(c == 'E') {
                print("S");
            } else {
                print("E");
            }
        }
        print("\n")
       
   }
}
