#include<stdio.h>
main()
{
    int n,i,b;
    char c[100];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&b);
        int x[b][b];
        scanf("%s",&c);
        for(j=0;j<strlen(c);j++)
        {
            if(strcmp(c[i],"E")==0)
                
        }
    }
}