#include<stdio.h>
#include<conio.h>
 int main()
 {
 int n,a,b,c;
 printf("Enter the amount to be given:");
 scanf("%d",&n);
 printf("enter the amount of both checks:");
 scanf("%d %d",&a,&b);
 
 if(a,b!=4)
 {
 c=a+b;
 a+b=n;
 n=c;
 
 printf("the total amount is ");
 scanf("%d",&c);
 }
 else
 {
 printf("Not valid");
 }
 }