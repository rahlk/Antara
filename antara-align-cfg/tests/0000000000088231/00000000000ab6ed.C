#include<stdio.h>
int main()
{
    int n,temp,num;
    scanf("%d",&n);
    temp = n;
    for(i=1;i<=n;i++)
    {
    a = num/2;
    b = num-b;
    printf("Case #%d: %d %d",i,a,b);
    }
    return 0;
}