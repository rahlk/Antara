#include<stdio.h>
#include<stdlib.h>
void main()
{
int A,B,N;
int choice,pats;
if (pts>N)
{ 
    printf("enter the choice based on the A and B values ");
    scanf(&choice);
    switch(choice) 
    {
        case 1:
        do {
             A +B =N;
            }while(A==B);
            break;
        case 2:
        do { 
             A+B =N ;
           }while(A>B);
           break;
        case 3:
        do {
             A+B=N;
           }while (A<B);
           break;
        
    }
}
}