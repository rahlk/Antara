#include<stdio.h>
#include<math.h>
int main()
{
    long int t;
    scanf("%ld",&t);
    for(long int test=1;test<=t;test++)
    {
        unsigned long long int n;
        long long int num[];
        int l;
        scanf("%llu",&n);
        scanf("%d",&l);
        for(int j=0;j<l;j++)
        {
            scanf("%lld",&a[j]);
        }
        int temp=1,n2[];
        for(j=0;j<l;j++)
        {
            for (long int i = 3; i <= sqrt(n[j]); i = i+2) 
            { 
                while (n[j]%i == 0) 
                { 
                    x=i;
                    n = n/i; 
                } 
            } 
            if (temp==1
            {
               n2[0]=x;
               n2[temp]=n[j]
            }
            else if (n[temp-1]==x)
            n2[temp]=n[j];
            else
            n2[temp]=x;
            temp++
            
            
        
    
    n3=n2
    for(i=0)
}