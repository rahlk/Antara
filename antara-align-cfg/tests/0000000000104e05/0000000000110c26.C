#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main()
{
    int i,j,n,co;
    char str[n];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        if(str[i]=str[i+1])
        {
            printf("%d",co);
        }
    }
}